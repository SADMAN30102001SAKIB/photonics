Isn't it possible to create a machine learning model on a research paper that does some stubs on the topics? I mean, you have to care about that. All you need to care about is the data. In that final, there is the data set, which looks something like this: for each index, let's say indicative index 1.1, it will generate a lot of y-axis values, I mean 50-60 values for the y-axis. The x-axis values are known; they are constant; we don't have to worry about that, but the y-axis values are generated from the reflective index. The research paper writer has simulated the y-axis values for each reflective index, I mean 1.33, 1.34, 1.35, 1.4, 1.41, etc., basically increasing the reflective index by 0.01. He had found the y-axis values from the simulation software and then drew the graph based on the y-axis values, and the x-axis values are known.

The thing is that, in the simulation, they can find values. In order to find a value, we need the simulation software. Can we create a machine learning model where it doesn't even have to be a machine learning model; it can be statistics or any other mathematical stuff? If it is machine learning, then it is good, because then my research will be very good and able to publish. Can't it be made so that, if I enter any random reflective index value, let's say 1.35896, based on that reflective index, the y-axis values will be auto-generated? The machine learning model will be trained on the given data set that exists in the research paper, which was generated by simulation, and then the machine learning model can predict, for any random reflective index, it can draw me the graph. Can we do that? It will predict all the y-axis values, and it needs to be so much accurate that, if we give that reflective index in the simulation software, then the values that we get from that simulation software for the y-axis, we need almost similar types of values from the machine learning model. I want maximum accuracy, and also I don't want very complex machine learning stuff. I mean, I am very new; I don't have any idea about machine learning, so you need to guide me because I literally have zero idea about machine learning. 

Yes, absolutely. In fact, what you're describing is often easier than many beginner machine-learning projects because you already have simulated data and a well-defined input/output relationship.

Let me restate the problem mathematically:

Input: Refractive index 
n
n (for example 1.33, 1.34, 1.35, ...)

Known: X-axis values (fixed)

Output: A whole curve (50–60 Y-values)

So your dataset looks like:

Refractive Index	Y₁	Y₂	Y₃	...	Y₆₀
1.33	...	...	...	...	...
1.34	...	...	...	...	...
1.35	...	...	...	...	...
...	...	...	...	...	...
The model learns:

n
→
[
Y
1
,
Y
2
,
Y
3
,
.
.
.
,
Y
60
]
n→[Y 
1
​
 ,Y 
2
​
 ,Y 
3
​
 ,...,Y 
60
​
 ]
Then if you enter:

n
=
1.35896
n=1.35896
the model predicts:

[
Y
1
,
Y
2
,
Y
3
,
…
,
Y
60
]
[Y 
1
​
 ,Y 
2
​
 ,Y 
3
​
 ,…,Y 
60
​
 ]
and you draw the graph using the known X-values.

The first question: Do you even need Machine Learning?
Maybe not.

If the curves change smoothly as refractive index changes (which is usually true in optics), then a simple interpolation method may achieve extremely high accuracy.

For example:

Simulation data at 1.35

Simulation data at 1.36

For 1.35896, you can interpolate between them.

This is called:

Linear interpolation

Cubic spline interpolation

Polynomial interpolation

Many published engineering papers use interpolation instead of neural networks when the relationship is smooth.

The second question: Can Machine Learning do it?
Yes.

Actually this is a regression problem.

Input:

n
n
Output:

60
-dimensional vector
60-dimensional vector
A very small neural network can learn this.

Example:

Input Layer: 1 neuron (refractive index)

Hidden Layer: 32 neurons

Hidden Layer: 32 neurons

Output Layer: 60 neurons
Output neuron 1 predicts Y₁.

Output neuron 2 predicts Y₂.

...

Output neuron 60 predicts Y₆₀.

This is called multi-output regression.

The third question: Will it be accurate enough?
This depends on the amount of data.

Suppose you only have:

1.33
1.34
1.35
1.36
1.37
That's only 5 training samples.

Machine learning will probably not be the best choice.

But suppose you simulate:

1.300
1.301
1.302
...
1.500
That's 201 samples.

Now ML can learn very accurately.

What I would recommend for a publishable paper
A common workflow is:

Step 1
Generate simulation data.

Example:

Refractive Index: 1.30 → Curve
Refractive Index: 1.31 → Curve
...
Refractive Index: 1.50 → Curve
Step 2
Split data:

80% training

20% testing

Step 3
Train a simple model:

Random Forest Regressor

XGBoost Regressor

Small Neural Network

Step 4
Compare with simulation software.

Metrics:

R
2
R 
2
 
M
A
E
MAE
R
M
S
E
RMSE
If:

R
2
>
0.99
R 
2
 >0.99
then the model is reproducing the simulation very closely.

A more advanced idea
Instead of predicting 60 points directly, predict the shape of the curve.

Many optical curves are smooth.

You can compress each curve using:

Principal Component Analysis (PCA)

For example:

60 values
↓
5 PCA coefficients
Then train ML to predict only 5 coefficients.

Finally reconstruct the 60-point curve.

This often gives:

Better accuracy

Smaller model

Faster prediction

This approach is very publishable.

What would impress reviewers most?
Not:

"I trained a neural network."

But:

"We developed a surrogate model that replaces computationally expensive simulations and predicts the spectral response for arbitrary refractive indices in milliseconds."

That's a common and respected research direction in optics, photonics, RF engineering, fluid dynamics, and materials science.

To tell you whether interpolation, PCA, Random Forest, XGBoost, or a neural network is the best choice, I would need to know:

How many refractive-index samples does the paper provide?

How many Y-values are in each curve?

What do the axes represent (wavelength vs transmission, absorbance, reflectance, etc.)?

Do you have the raw numerical tables, or only graphs from the paper?

With that information, I can suggest the simplest method that still has a good chance of being publishable.


Phase 1: Prepare the Data
Open your Excel file.

Arrange the data so that:

Column 1 = RI

Columns 2–62 = the 61 confinement-loss values.

Verify:

8 RI values for training.

3–4 RI values reserved for testing only.

Never use the test RI values during training.

Phase 2: Visual Inspection
Plot all training curves.

Check whether curves change smoothly as RI increases.

If curves look smooth, continue.

Phase 3: Build Baseline Models
Implement:

Model A
Cubic Spline Interpolation

Model B
Polynomial Regression

Try degree 2, 3, 4, and 5.

Keep the best degree.

Model C
Support Vector Regression (SVR)

RBF kernel.

Model D
Gaussian Process Regression (GPR)

RBF kernel.

Start with default settings.

Phase 4: Train
Train each model using only the 8 training RI values.

For each model:

Input:
RI

Output:
61 confinement-loss values
Phase 5: Predict
Predict the 3–4 unseen test RI values.

Example:

Training:
1.33
1.34
...
1.40

Testing:
1.345
1.365
1.385
Generate the full 61-point curve for each test RI.

Phase 6: Evaluate
For every test RI and every model calculate:

R²

MAE

RMSE

Create a comparison table:

Model	R²	MAE	RMSE
Spline			
Polynomial			
SVR			
GPR			
Phase 7: Create Figures
For each test RI:

Plot actual simulation curve.

Plot predicted curve.

Example:

Actual (Simulation)
Predicted (GPR)
same graph.

Also create a bar chart comparing:

R²

MAE

RMSE

across models.

Phase 8: Select the Winner
Choose the model with:

Highest R²

Lowest MAE

Lowest RMSE

Most likely:

Cubic Spline

or GPR

will win.

Phase 9: Research Contribution
State that the chosen model acts as a surrogate for the simulation software.

Emphasize:

No simulation required after training.

Arbitrary RI values can be predicted instantly.

Significant computational time reduction.

Phase 10: Paper Structure
Introduction

Related Work

Dataset Generation (simulation setup)

Methodology

Spline

Polynomial

SVR

GPR

Results

Tables

Graphs

Metrics

Discussion

Conclusion

One important improvement
Instead of training 61 separate models, first try:

Input:
RI

Output:
61 values simultaneously
If that becomes difficult, use:

PCA → keep 3–5 components → train GPR/SVR on PCA coefficients → reconstruct the curve.

But for your first version, train directly on the 61 outputs and get the pipeline working before introducing PCA.

Branched from Branch · ML Model for Data Prediction


image(1).png
this is the graph from the training dataset

This graph changes my confidence level quite a bit.

What I see:

Multiple resonance peaks.

Peak position shifts as RI changes.

Peak height changes significantly.

Peak width changes.

The behavior still looks smooth, but it's not a simple monotonic curve.

This is much more realistic for a photonic sensor than the simplified case we were discussing.

What this means for model selection
I would now rank the candidates differently:

Tier 1 (most promising)
Gaussian Process Regression (GPR)

Cubic Spline Interpolation

Tier 2
SVR

Tier 3
Polynomial Regression

Looking at the graph, I would not expect a global polynomial to model those shifting resonances very well.

The most important observation
I would not train 61 independent models immediately.

Because the 61 y-values are highly correlated.

When the resonance peak moves:

Y1 changes
Y2 changes
Y3 changes
...
Y61 changes
together.

The curve shape contains much less information than 61 completely independent numbers.

If I were supervising this as a thesis
I would recommend:

Approach A (simple)
Train directly:

Input:
RI

Output:
61 values
using GPR.

Get results.

This is easiest.

Approach B (more publishable)
Apply PCA first.

61 values
↓
PCA
↓
3-5 coefficients
Then train:

RI → PCA coefficients
using GPR.

Finally reconstruct the curve.

This is a very common surrogate-modeling workflow.

About hybrids
Looking at this graph:

No.

I see absolutely no reason yet to build a hybrid.

A hybrid is something you consider after:

Spline tested
SVR tested
GPR tested
and they all have a specific weakness.

You are nowhere near that stage.

What I would do next
In order:

Experiment 1
Cubic Spline

Predict all hidden RI values.

Record:

R²

MAE

RMSE

Experiment 2
GPR

Same metrics.

Experiment 3
SVR

Same metrics.

Then compare.

Honestly, with only:

8 training RI values
I would be shocked if you needed anything more complicated than that.

One thing I need to know
This graph suggests the peaks move as RI changes.

How many training RI values are there exactly?

For example:

1.33
1.34
1.35
1.36
1.37
1.38
1.39
1.40
or something else?

The exact RI spacing matters a lot.

If the spacing is uniform and only 0.01 apart, then predicting something like:

1.365
is a relatively easy interpolation problem.

If the spacing is irregular or much larger, the difficulty changes considerably.


it 
"1.33
1.34
1.35
1.36
1.37
1.38
1.39
1.40"

and the peak isn't perfect circle like smooth

That's actually normal and not a problem.

In photonic crystal fiber, SPR, plasmonic, and resonant optical sensor papers, the confinement-loss peak is often:

asymmetric,

skewed,

slightly noisy from numerical simulation,

not a perfect Gaussian,

not a perfect Lorentzian.

So the fact that the peak isn't a smooth bell shape doesn't worry me.

What matters more is:

Does the peak move smoothly as RI changes?

From the graph, it appears:

RI ↑
Peak wavelength shifts right
Peak amplitude increases
Peak shape changes gradually
That's the key thing ML/interpolation can learn.

Now that you've told me the training RIs are:

1.33
1.34
1.35
1.36
1.37
1.38
1.39
1.40
I can be more specific.

Predicting:

1.345
1.355
1.365
1.375
is a pure interpolation problem.

You're never asking the model to leave the known RI range.

That's the easiest and safest scenario.

If I were reviewing this paper, my first question would be:

Why use ML at all when the training points are uniformly spaced and the task is interpolation?

That's why your comparison against Cubic Spline is important.

Not because spline is ML.

Because it's the strongest classical competitor.

My current recommendation
Do exactly this:

Method	Keep?
Cubic Spline	Yes
GPR	Yes
SVR	Optional
Polynomial Regression	Skip if short on time
Neural Network	No
Hybrid Models	No
Honestly, if you're busy, I'd even reduce it to:

Method 1: Cubic Spline
Method 2: Gaussian Process Regression
and do the comparison properly.

A strong comparison between 2 well-justified methods is often better than a weak comparison between 10 methods.

One more thing
Looking at your graph, I would not evaluate only the full 61-point curve.

I'd also extract sensor metrics:

Resonance wavelength

Maximum confinement loss

Sensitivity

because reviewers in optical sensing usually care about those physical quantities.

For each test RI:

Predict the whole curve.

Find the predicted resonance peak wavelength.

Compare against simulation.

That often looks more convincing than only reporting R² on the entire curve.

If I had your dataset today, my first experiment would be:

Train: RI = 1.33–1.40

Test:
1.345
1.365
1.385

Compare:
- Cubic Spline
- GPR

Metrics:
- R²
- MAE
- RMSE

Also compare:
- Peak wavelength error
- Peak loss error
That would tell me within a few hours whether the project has publication potential.